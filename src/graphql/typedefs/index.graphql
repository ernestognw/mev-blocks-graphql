type Query {
  # Blocks
  blocks(
    miner: String
    from: String
    before: String
    limit: Int
  ): [Block]!
  block(block_number: Int!): Block

  # Transactions
  transactions(
    before: String
    limit: Int
  ): [Transaction]!
}
